# Brand fonts for make_flyer.py (not committed — download from Google Fonts)

Place these files here:

- `Pacifico-Regular.ttf`
- `OpenSans-Variable.ttf` (Open Sans variable)
- `PlayfairDisplay-Variable.ttf` (Playfair Display variable)

On macOS the script falls back to Georgia/Arial if these are missing.
